import { Injectable } from '@nestjs/common';

@Injectable()
export class EventsService {
  // TODO Loric — findAll(filters): liste avec pagination
  // TODO Loric — findOne(id)
  // TODO Loric — create(dto, userId)
  // TODO Loric — update(id, dto, userId)
  // TODO Loric — remove(id, userId)
  // TODO Loric — registerToEvent(eventId, userId): crée une Registration, déclenche paiement si payant
}
