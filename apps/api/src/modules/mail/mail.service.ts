import { Injectable } from '@nestjs/common';

@Injectable()
export class MailService {
  // TODO Wessim — sendWelcome(user): email de bienvenue après inscription
  // TODO Wessim — sendEventConfirmation(user, event): confirmation d'inscription à un événement
  // TODO Wessim — sendPaymentReceipt(user, event, amount): reçu de paiement
  // TODO Wessim — sendEventReminder(user, event): rappel 24h avant l'événement
}
