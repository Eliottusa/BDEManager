import { Injectable } from '@nestjs/common';

@Injectable()
export class UsersService {
  // TODO Jordan — findById(id): récupérer depuis Redis ou DB
  // TODO Jordan — findByEmail(email): pour l'auth
}
