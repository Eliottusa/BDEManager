// TODO Loric — Formulaire création d'événement (ORGANIZER uniquement)
// Champs : titre, description, date début/fin, capacité, prix, adresse (autocomplétion)
// TODO Eliott — Composant AddressAutocomplete → GET /api/v1/geo/search?q=...
export default function CreateEventPage() {
  return <main>Créer un événement</main>;
}
