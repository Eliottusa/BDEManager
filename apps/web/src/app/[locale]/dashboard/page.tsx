// TODO Eliott — Dashboard : mes inscriptions, mes événements organisés
// TODO Wessim — afficher les notifications temps réel (socket.io-client)
export default function DashboardPage() {
  return <main>Dashboard</main>;
}
