// TODO Timéo — Page de retour après paiement Stripe réussi
// Stripe redirige ici avec ?session_id=... après checkout
export default function CheckoutSuccessPage() {
  return <main>Paiement confirmé</main>;
}
